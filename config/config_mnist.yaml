dataset: "mnist"
nb_classes: 10
initialized: false
normalization_method: "division"
file_save: ""
model:
  loss: "CEL"

  layers:
    - type: "Convolutional"
      number_kernels: 8
      size_kernel: 3
      stride: 1
      padding: 1
      initialization: "he"

    - type: "Activation"
      function: "relu"

    - type: "MaxPool"
      size_kernel: 2
      stride: 2

    - type: "Convolutional"
      number_kernels: 16
      size_kernel: 3
      stride: 1
      padding: 1
      initialization: "he"

    - type: "Activation"
      function: "relu"

    - type: "MaxPool"
      size_kernel: 2
      stride: 2

    - type: "Flattening"

    - type: "Dense"
      number_neurons: 64
      initialization: "he"

    - type: "Activation"
      function: "relu"

    - type: "Dropout"
      drop_rate: 0.1

    - type: "Dense"
      number_neurons: 10

training:
  validation_part: 0.2
  early_stop: true
  patience: 5        
  min_epoch: 8       
  epochs: 1
  batch_size: 64
  optimizer: 
    type: "Adam"
    learning_rate: 0.001
    momentum_rate: 0.9
    velocity_rate: 0.999

visualization:
  show_plots: true
  minus_y: false

CAM_image: null